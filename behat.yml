default:
    suites:
        default:
            contexts:
                - FeatureContext
            paths:
                - %paths.base%/features
    extensions:
        Behat\Symfony2Extension: ~
        Behat\MinkExtension:
            default_session: symfony
            symfony: ~
            base_url: http://localhost:8000
